<div align='center'>

# JavaScript Interview Question
</div>

## 📚 Table of Contents

1. [What is the Higher Order function & callback function in JavaScript?]()
1. [What is Hoisting & closure in JavaScript?]()
1. [What is Scope in javascript?]()
1. [What is the difference between Call, Apply, and Bind?]()
1. [What is the difference between undefined and null ?]()
1. [What is a cookie?]()
1. [What is a promise ?]()
1. [What is an event loop? How does javaScript handle asynchronous tasks?]()
1. [What is a prototype chain ?]()
1. [How can you eliminate duplicate values from a JavaScript array?]()

<br>



## 1️⃣ Higher Order Function & Callback Function কী?


## Higher Order Function কী? (JavaScript)

**সহজ ভাষায়:**
যে ফাংশন **অন্য ফাংশনকে ইনপুট হিসেবে নেয়** অথবা **অন্য ফাংশন রিটার্ন করে**, তাকে **Higher Order Function** বলে।

👉 মানে, এই ফাংশনটি **ফাংশনের সাথে কাজ করে**।

### সহজ উদাহরণ:

```javascript
function add(a, b) {
  return a + b;
}

function calculator(operation) {
  return operation(5, 3);
}

calculator(add); // 8
```

এখানে

* `calculator` হলো **Higher Order Function**
* কারণ এটি `add` নামের একটি **ফাংশনকে প্যারামিটার হিসেবে নিয়েছে**

### বাস্তব জীবনের উদাহরণ:

রিমোট কন্ট্রোল নিজে টিভি চালায় না,
বরং টিভিকে **কন্ট্রোল করে**।
Higher Order Function ঠিক তেমনই।

### কিছু পরিচিত Higher Order Function:

* `map()`
* `filter()`
* `reduce()`
* `forEach()`

```javascript
numbers.map(function(n) {
  return n * 2;
});
```

---

## Callback Function কী?

**সহজ ভাষায়:**
যে ফাংশনকে **অন্য ফাংশনের ভেতরে পাঠানো হয়** এবং পরে **সেখানে কল করা হয়**, তাকে **Callback Function** বলে।

### সহজ উদাহরণ:

```javascript
function greet(name, callback) {
  callback(name);
}

function sayHello(name) {
  console.log("Hello " + name);
}

greet("Irfan", sayHello);
```

এখানে

* `sayHello` হলো **Callback Function**
* কারণ এটি `greet` ফাংশনের ভেতরে পাঠানো হয়েছে এবং সেখানে কল হয়েছে

### বাস্তব জীবনের উদাহরণ:

আপনি কাউকে বলেন,
“কাজ শেষ হলে আমাকে ফোন দিও।”
এই “পরে কল করো” ব্যাপারটাই হলো **Callback**।

---

## Higher Order Function ও Callback এর সম্পর্ক

* **Higher Order Function** → ফাংশন নেয় বা ফাংশন রিটার্ন করে
* **Callback Function** → যে ফাংশনটি পাস করা হয়

```javascript
setTimeout(function() {
  console.log("Done!");
}, 1000);
```

এখানে

* `setTimeout` → Higher Order Function
* `function() { ... }` → Callback Function

---

## ইন্টারভিউতে বলার জন্য এক লাইনের উত্তর

> Higher Order Function হলো এমন ফাংশন যা অন্য ফাংশনকে প্যারামিটার হিসেবে নেয় বা রিটার্ন করে, আর Callback Function হলো সেই ফাংশন যেটা অন্য ফাংশনের ভেতরে পাঠানো হয় এবং পরে এক্সিকিউট হয়।


---

## 2️⃣ Hoisting & Closure কী?

## Hoisting কী? (JavaScript)

**সহজ ভাষায়:**
JavaScript কোড রান করার আগে কিছু **ভ্যারিয়েবল ও ফাংশনকে উপরে তুলে ধরে**—এটাকেই **Hoisting** বলে।

👉 মনে রাখবে:

* **Declaration hoist হয়**
* **Value hoist হয় না**

### উদাহরণ ১: `var` দিয়ে Hoisting

```javascript
console.log(a);
var a = 10;
```

এটা error দেবে না, আউটপুট হবে:

```text
undefined
```

কারণ JavaScript এটা এমনভাবে দেখে:

```javascript
var a;
console.log(a);
a = 10;
```

### `let` এবং `const` এর ক্ষেত্রে

```javascript
console.log(b);
let b = 20;
```

👉 এটা **error দেবে**, কারণ `let` ও `const` hoist হলেও ব্যবহার করা যায় না assignment-এর আগে।

### Function Hoisting

```javascript
hello();

function hello() {
  console.log("Hello!");
}
```

👉 এটা ঠিকভাবে কাজ করবে, কারণ **function declaration পুরোপুরি hoist হয়**।

---

## Closure কী? (JavaScript)

**সহজ ভাষায়:**
**Closure** হলো এমন একটি ফাংশন, যা তার **বাইরের ফাংশনের ভ্যারিয়েবল মনে রাখতে পারে**, এমনকি বাইরের ফাংশন শেষ হয়ে গেলেও।

### সহজ উদাহরণ:

```javascript
function outer() {
  let count = 0;

  function inner() {
    count++;
    console.log(count);
  }

  return inner;
}

const counter = outer();
counter(); // 1
counter(); // 2
```

🧠 এখানে কী হচ্ছে?

* `outer()` শেষ হয়ে গেছে
* কিন্তু `inner()` এখনো `count` মনে রাখছে
  👉 এটাই **Closure**

### বাস্তব জীবনের উদাহরণ:

ধরো তুমি একটা **ব্যাগ** বানালে (outer function)
ভেতরে কিছু জিনিস রেখে দিলে (variables)
ব্যাগ বাইরে নিয়ে গেলেও জিনিসগুলো থেকে যায়
👉 ব্যাগটাই হলো **Closure**



## 3️⃣ Scope কী?

**Scope** মানে হলো—কোথা থেকে কোন variable অ্যাক্সেস করা যাবে।

### Types:

* **Global Scope** – সব জায়গা থেকে পাওয়া যায়
* **Function Scope** – শুধু function এর ভিতরে
* **Block Scope** – `{ }` এর ভিতরে (`let`, `const`)

---

## 4️⃣ Call, Apply, Bind এর পার্থক্য কী?

সবগুলোই function এর `this` সেট করতে ব্যবহার হয়।

* **call()** → argument আলাদা আলাদা
* **apply()** → argument array আকারে
* **bind()** → সাথে সাথে call হয় না, নতুন function return করে

**সহজভাবে:**

> call = এখনই, আলাদা argument
> apply = এখনই, array argument
> bind = পরে call করার জন্য

---

## 5️⃣ undefined ও null এর পার্থক্য কী?

জাভাস্ক্রিপ্টে **`undefined`** আর **`null`** দেখতে কাছাকাছি হলেও আসলে এদের মানে আলাদা। ১০ বছরের বাচ্চার মতো সহজ করে বুঝাই 😊

---

### 🔹 `undefined` কী?

`undefined` মানে হলো
👉 **“এখানে কিছু থাকার কথা ছিল, কিন্তু এখনো কিছুই নেই।”**

যখন তুমি কোনো ভ্যারিয়েবল বানাও কিন্তু তাকে কোনো মান দাও না, তখন সে নিজে থেকেই `undefined` হয়।

```js
let x;
console.log(x); // undefined
```

এখানে `x` আছে, কিন্তু তার ভেতরে কিছু রাখা হয়নি।

আরও উদাহরণ:

```js
function test() {}
console.log(test()); // undefined
```

ফাংশন কিছু return না করলে সেটাও `undefined` হয়।

---

### 🔹 `null` কী?

`null` মানে হলো
👉 **“এখানে আমি ইচ্ছা করে কিছুই রাখিনি।”**

মানে, প্রোগ্রামার নিজে বলছে—এই ভ্যারিয়েবলের ভ্যালু এখন খালি।

```js
let y = null;
console.log(y); // null
```

এখানে `y` আছে, আর আমরা ইচ্ছা করে বলেছি: এর ভেতরে কিছু নেই।

---

### 🧠 সহজভাবে মনে রাখার ট্রিক

| বিষয়    | undefined           | null                        |
| ------- | ------------------- | --------------------------- |
| কে দেয়? | JavaScript নিজে     | প্রোগ্রামার                 |
| মানে    | এখনো মান দেওয়া হয়নি | ইচ্ছা করে খালি রাখা         |
| টাইপ    | undefined           | object (JS এর পুরনো বাগ 😄) |

```js
typeof undefined; // "undefined"
typeof null;      // "object"
```

---

### 🍬 বাস্তব জীবনের উদাহরণ

ধরো একটা বাক্স 📦

* **undefined** 👉 বাক্স আছে, কিন্তু ভেতরে কী থাকবে এখনো ঠিক করা হয়নি
* **null** 👉 বাক্স আছে, আর ভেতরে কিছুই নেই—জানিয়ে দেওয়া হয়েছে

---

### 🧩 সংক্ষেপে এক লাইনে

* **undefined** = মান এখনো আসেনি
* **null** = মান ইচ্ছা করে নেই

এটা বোঝা গেলে জাভাস্ক্রিপ্ট অনেক সহজ হয়ে যায় 🌟

<br><div align="center"><strong>─────── ✦ x ✦ ───────</strong></div><br><br>


# 6️⃣ Cookie কী?

**Cookie** মানে হলো 👉 **একটা ছোট তথ্যের প্যাকেট**, যেটা কোনো ওয়েবসাইট তোমার ব্রাউজারে রেখে দেয়।

১০ বছরের বাচ্চার মতো সহজ করে বলি 😊

---

### 🍪 Cookie কী?

যখন তুমি কোনো ওয়েবসাইটে যাও, সেই ওয়েবসাইট তোমার কম্পিউটার বা মোবাইলে **ছোট ছোট তথ্য** রেখে দেয়।
এই ছোট তথ্যগুলোই হলো **cookie**।

---

### 🤔 Cookie কেন দরকার?

Cookie ওয়েবসাইটকে **মনে রাখতে সাহায্য করে**।

যেমন ধরো 👇

* তুমি লগইন করেছ → cookie মনে রাখে তুমি কে
* তুমি ভাষা English বেছে নিয়েছ → cookie মনে রাখে ভাষা
* অনলাইন শপে জিনিস কার্টে দিয়েছ → cookie কার্ট মনে রাখে

---

### 🧠 সহজ উদাহরণ

ধরো তুমি একটা স্কুলে গেলে, আর গেটম্যান তোমার হাতে একটা টোকেন দিল 🎟️

* টোকেন থাকলে → সবাই জানে তুমি আগেই ঢুকেছ
* টোকেন না থাকলে → আবার পরিচয় দিতে হবে

👉 এই টোকেনটাই হলো **cookie**

---

### 🧩 Cookie-তে কী থাকে?

Cookie-তে থাকতে পারে:

* তোমার user id
* login status
* theme বা language
* ছোট সেটিংস তথ্য

⚠️ Cookie-তে **পাসওয়ার্ড রাখা উচিত না**

---

### 🍪 Cookie এর ধরন (খুব সহজ)

* **Session cookie**: ব্রাউজার বন্ধ করলে মুছে যায়
* **Persistent cookie**: কিছুদিন থাকে (যেমন 7 দিন, 30 দিন)

---

### 🛑 Cookie কি ক্ষতিকর?

না, সাধারণত না।
কিন্তু:

* কিছু cookie বিজ্ঞাপনের জন্য ব্যবহার হয়
* তাই ওয়েবসাইটে লেখা আসে: **“Accept cookies?”**

---

### 🧠 এক লাইনে মনে রাখো

**Cookie = ওয়েবসাইটের ছোট স্মৃতির কাগজ 📝**

এতে ওয়েবসাইট তোমাকে বারবার নতুন মানুষ মনে করে না 😄

<br><div align="center"><strong>─────── ✦ x ✦ ───────</strong></div><br><br>


# 7️⃣ Promise কী?

### 🔷 Promise কী? (JavaScript)

**Promise** মানে হলো 👉 **ভবিষ্যতে কিছু একটা হবে—এই কথা দিয়ে রাখা প্রতিশ্রুতি**।

১০ বছরের বাচ্চার মতো সহজ করে ভাবি 😊

---

### 🧠 সহজ গল্প

ধরো, তুমি তোমার বন্ধুকে বললে—
👉 “আমি আজ বিকেলে তোমাকে চকলেট দেব 🍫”

এখন ৩টা অবস্থা হতে পারে:

1. **দেওয়া হলো** → কাজ সফল ✅
2. **দেওয়া হলো না** → কাজ ব্যর্থ ❌
3. **এখনো সময় আসেনি** → অপেক্ষা ⏳

JavaScript-এ এই তিনটা অবস্থাই হলো **Promise**।

---

### 🔹 Promise এর ৩টি অবস্থা

1. **Pending** – এখনো কাজ চলছে
2. **Fulfilled** – কাজ সফল হয়েছে
3. **Rejected** – কাজ ব্যর্থ হয়েছে

---

### 🔹 Promise কেন দরকার?

JavaScript অনেক সময় **দেরিতে কাজ শেষ করে**, যেমন:

* server থেকে data আনা
* file পড়া
* API call করা

Promise বলে দেয়:
👉 “কাজ শেষ হলে আমি জানাবো।”

---

### 🔹 সহজ Promise উদাহরণ

```js
let promise = new Promise((resolve, reject) => {
  let success = true;

  if (success) {
    resolve("কাজ সফল হয়েছে");
  } else {
    reject("কাজ ব্যর্থ হয়েছে");
  }
});
```

---

### 🔹 Promise ব্যবহার করা (`then`, `catch`)

```js
promise
  .then(result => {
    console.log(result);
  })
  .catch(error => {
    console.log(error);
  });
```

* **then** → কাজ সফল হলে
* **catch** → কাজ ব্যর্থ হলে

---

### 🍭 খুব সহজ উদাহরণ

ধরো তুমি অনলাইনে খাবার অর্ডার করেছ 🍔

* অর্ডার দেওয়া → Promise
* খাবার এলো → `then`
* খাবার এলো না → `catch`

---

### 🧠 এক লাইনে মনে রাখো

**Promise = ভবিষ্যতের কাজের জন্য JavaScript-এর প্রতিশ্রুতি 🤝**

Promise বুঝলে `async`, `await` বোঝা অনেক সহজ হয়ে যায় 🌟

<br><div align="center"><strong>─────── ✦ x ✦ ───────</strong></div><br><br>


# 8️⃣ Event Loop কী? JavaScript async কাজ কীভাবে handle করে?

### 🔁 Event loop কী? JavaScript কীভাবে asynchronous কাজ সামলায়?


![Image](https://i.sstatic.net/BTm1H.png)

![Image](https://felixgerschau.com/static/79486d91b22a7c1b4044fce88a4cae20/5a190/js-event-loop-explained.png)

![Image](https://media2.dev.to/dynamic/image/width%3D1600%2Cheight%3D900%2Cfit%3Dcover%2Cgravity%3Dauto%2Cformat%3Dauto/https%3A%2F%2Fdev-to-uploads.s3.amazonaws.com%2Fuploads%2Farticles%2Flqhhnr6n1w1l13g75ki0.png)

---

## 🧠 Event loop কী?

**Event loop** হলো JavaScript-এর **ম্যানোজার** 👮
ওর কাজ—
👉 কখন কোন কাজ আগে চলবে, আর কোনটা পরে চলবে—এই সিদ্ধান্ত নেওয়া।

---

## 🧩 JavaScript-এ কাজের জায়গাগুলো

সহজভাবে ৩টা জায়গা ভাবো:

1. **Call Stack** 🧱
   এখানে এখনই চলা কাজ থাকে (সাধারণ কোড)

2. **Callback / Task Queue** 📬
   দেরিতে হওয়া কাজ এখানে অপেক্ষা করে (যেমন `setTimeout`)

3. **Event Loop** 🔄
   দেখে—Call Stack খালি হলেই Queue থেকে কাজ এনে চালায়

---

## 🍕 গল্প দিয়ে বুঝি

ধরো তুমি রান্নাঘরে আছো:

* তুমি ভাত বসালে 🍚 → (সময় লাগবে)
* অপেক্ষা না করে তুমি সবজি কাটছো 🥕 → (অন্য কাজ)
* ভাত হয়ে গেলে 🔔 → তখন তুমি ভাত নামাও

👉 **তুমি বসে থাকোনি**,
👉 **Event loop** ঠিক এই কাজটাই করে।

---

## 🔹 ছোট কোড উদাহরণ

```js
console.log("১");

setTimeout(() => {
  console.log("৩");
}, 0);

console.log("২");
```

📌 Output হবে:

```
১
২
৩
```

👉 কারণ:

* `console.log` → Call Stack-এ সাথে সাথে চলে
* `setTimeout` → Queue-তে যায়
* Event loop পরে Queue থেকে এনে চালায়

---

## ⚙️ Asynchronous কাজ কীভাবে হ্যান্ডেল হয়?

* API call
* `setTimeout`
* Promise
* `fetch`

এই কাজগুলো:

1. শুরু হয়
2. পাশে রেখে দেওয়া হয়
3. শেষ হলে Queue-তে আসে
4. Event loop সুযোগ পেলে চালায়

---

## 🧠 এক লাইনে মনে রাখো

**Event loop = JavaScript-এর ট্রাফিক পুলিশ 🚦**
সে বলে দেয়—
👉 “এখন এই কাজ, তারপর ওই কাজ”

এই জন্যই JavaScript একসাথে অনেক কাজ করতে পারে,
ওয়েবসাইট কখনো ফ্রিজ হয় না 🚀

<br><div align="center"><strong>─────── ✦ x ✦ ───────</strong></div><br><br>

# 9️⃣ Prototype Chain কী?

### 🔗 Prototype Chain কী? (JavaScript)

১০ বছরের বাচ্চার মতো সহজ করে বলছি 😊

![Image](https://djcodes.wordpress.com/wp-content/uploads/2015/11/protodiagram1.png)

![Image](https://i.sstatic.net/FPPdI.png)

![Image](https://i.sstatic.net/d4bDt.png)

---

## 🧠 Prototype Chain মানে কী?

**Prototype Chain** মানে হলো 👉
**একটা জিনিস নিজে কিছু না পেলে, সে তার “বাবা–দাদার” কাছে খুঁজে দেখে।**

JavaScript-এ এই **বাবা–দাদার লাইনটাই** হলো Prototype Chain।

---

## 🍭 সহজ গল্প

ধরো তুমি কিছু খুঁজছো—

1. আগে নিজের ব্যাগে দেখো 🎒
2. না পেলে বাবার ব্যাগে দেখো
3. তাও না পেলে দাদার ব্যাগে দেখো

👉 এই খোঁজার ধারাবাহিক পথটাই **Prototype Chain**।

---

## 🔹 JavaScript-এ কীভাবে হয়?

JavaScript-এ সব **object** এর একটা **prototype** থাকে।
কোনো property বা method না পেলে—

👉 নিজের মধ্যে খোঁজে <br> 
👉 না পেলে prototype-এ খোঁজে <br>
👉 তারপর prototype-এর prototype-এ <br>
👉 শেষে `null` এ গিয়ে থামে <br>

---

## 🔹 ছোট কোড উদাহরণ

```js
let person = {
  name: "Rahim"
};

console.log(person.toString());
```

👉 `person` এর ভেতরে `toString()` নেই <br>
👉 JavaScript খুঁজে পায় `Object.prototype`-এ <br>
👉 তাই কাজ করে

---

## 🔗 চেইনটা এমন দেখায়

```
person
  ↓
Object.prototype
  ↓
null
```

---

## 🧩 আরেকটা উদাহরণ (Array)

```js
let arr = [1, 2, 3];
arr.push(4);
```

👉 `push()` arr-এর নিজের না <br>
👉 আসে `Array.prototype` থেকে <br>
👉 তারপর `Object.prototype`

চেইন:

```
arr
 ↓
Array.prototype
 ↓
Object.prototype
 ↓
null
```

---

## 🧠 এক লাইনে মনে রাখো

**Prototype Chain = JavaScript-এর পারিবারিক উত্তরাধিকার 👨‍👩‍👧‍👦**  <br>
নিজে না পেলে → prototype → prototype-এর prototype <br>

এটা বুঝলে JavaScript-এর object আর class অনেক পরিষ্কার হয়ে যায় 🚀

<br><div align="center"><strong>─────── ✦ x ✦ ───────</strong></div><br><br>

# 🔟 JavaScript Array থেকে duplicate value কীভাবে remove করবেন?

JavaScript **Array থেকে duplicate value remove** করা মানে হলো <br>
👉 **একই জিনিস বারবার থাকলে, একবার রেখে বাকিগুলো বাদ দেওয়া**।

---

## ✅ সবচেয়ে সহজ ও জনপ্রিয় উপায় (Set ব্যবহার করে)

```js
let numbers = [1, 2, 2, 3, 4, 4, 5];

let uniqueNumbers = [...new Set(numbers)];

console.log(uniqueNumbers);
```

📌 Output:

```
[1, 2, 3, 4, 5]
```

### 🧠 কেন কাজ করে?

* **Set** কখনো duplicate রাখে না
* `...` (spread) দিয়ে আবার array বানানো হয়

👉 **এটাই সবচেয়ে modern ও safe উপায়** ✅

---

## 🔹 `filter()` ব্যবহার করে

```js
let arr = [1, 2, 2, 3, 3, 4];

let uniqueArr = arr.filter((value, index) => {
  return arr.indexOf(value) === index;
});

console.log(uniqueArr);
```

📌 Output:

```
[1, 2, 3, 4]
```

### 🧠 আইডিয়া

* প্রথমবার যেটা আসে → রাখে
* পরে যেগুলো আসে → বাদ দেয়

---

## 🔹 `reduce()` ব্যবহার করে (বোঝার জন্য)

```js
let arr = [1, 2, 2, 3];

let uniqueArr = arr.reduce((acc, value) => {
  if (!acc.includes(value)) {
    acc.push(value);
  }
  return acc;
}, []);

console.log(uniqueArr);
```

📌 Output:

```
[1, 2, 3]
```

---

## 🍭 সহজ উদাহরণ

ধরো তোমার কাছে চকলেট আছে 🍫
`[Dairy Milk, KitKat, KitKat, Perk]`

Duplicate remove করলে থাকবে:
`[Dairy Milk, KitKat, Perk]`

---

## 🧠 এক লাইনে মনে রাখো

* **Best & Easy** → `Set`
* **Logic শেখার জন্য** → `filter` / `reduce`

আজকাল real project-এ **`Set`-ই সবচেয়ে বেশি ব্যবহার হয়** 🚀

<br><div align="center"><strong>─────── ✦ x ✦ ───────</strong></div><br><br>