<div align='center'>

# TypeScript  Interview Question
</div>

## 📚 Table of Contents

1. [What are the three main primitive data types in TypeScript?]()
1. [What is an Interface in typescript?]()
1. [What is a type assertion in typescript?]()
1. [How many access modifiers are there in typeScript?]()
1. [What are getters/setters?]()
1. [How do you define a type alias in TypeScript? & Explain the purpose of type aliases in TypeScript.]()
1. [What is the difference between public, private, and protected access modifiers?]()
1. [Describe the difference between any and unknown types in Typescript. When would you use each?]()
1. [How do you handle null and undefined in TypeScript?]()
1. [Describe the difference between interface and type in TypeScript.]()

<br>


# 1️⃣ TypeScript-এর তিনটি main primitive data type কী?

TypeScript-এ **primitive data type** মানে হলো 👉 **সবচেয়ে সাধারণ ও ছোট ডেটা টাইপ**।

---

### 1️⃣ **string**

👉 লেখা বা শব্দ রাখার জন্য

```ts
let name: string = "Rahim";
```

* নাম
* বাক্য
* যেকোনো text

📌 উদাহরণ: `"Hello"`, `"Bangladesh"`

---

### 2️⃣ **number**

👉 সংখ্যা রাখার জন্য

```ts
let age: number = 10;
```

* 1, 2, 3
* 10.5
* −20

📌 TypeScript-এ আলাদা করে `int` বা `float` নেই—সবই `number`

---

### 3️⃣ **boolean**

👉 শুধু **হ্যাঁ / না**, **true / false**

```ts
let isStudent: boolean = true;
```

* true = ঠিক ✔️
* false = ভুল ❌

---

## 🧠 খুব সহজ মনে রাখার ট্রিক

* **string** → কথা 💬
* **number** → সংখ্যা 🔢
* **boolean** → হ্যাঁ / না 👍👎

---

## ⭐ এক লাইনে

**TypeScript-এর তিনটি main primitive data type হলো:
`string`, `number`, `boolean`**

এগুলো ঠিকভাবে বুঝলে TypeScript শেখা অনেক সহজ হয়ে যায় 🚀

<br><div align="center"><strong>─────── ✦ x ✦ ───────</strong></div><br><br>

# 2️⃣ TypeScript-এ Interface কী?


## 🧠 Interface মানে কী?

**Interface** হলো 👉 **একটা নকশা (blueprint)**
যেটা বলে দেয়—
👉 “এই object-এর ভেতরে কী কী থাকবে, আর কোনটার টাইপ কী হবে।”

এটা নিজে কিছু রাখে না,
👉 শুধু **নিয়ম বানায়**।

---

## 🏠 সহজ উদাহরণ

ধরো তুমি একটা বাড়ি বানাবে 🏡
নকশায় লেখা আছে:

* কয়টা রুম
* কয়টা দরজা
* কয়টা জানালা

👉 কিন্তু নকশা নিজে বাড়ি না—
Interface-ও ঠিক তেমনই।

---

## 🔹 Interface দিয়ে object বানানো

```ts
interface Person {
  name: string;
  age: number;
  isStudent: boolean;
}

let person1: Person = {
  name: "Rahim",
  age: 10,
  isStudent: true
};
```

👉 এখানে `Person` Interface বলছে:

* `name` হবে string
* `age` হবে number
* `isStudent` হবে boolean

নিয়ম ভাঙলে TypeScript error দেখাবে ❌

---

## 🔹 Optional property (থাকতেও পারে, নাও থাকতে পারে)

```ts
interface User {
  name: string;
  email?: string;
}

let user1: User = {
  name: "Karim"
};
```

👉 `email?` মানে email থাকলেও চলবে, না থাকলেও চলবে

---

## 🔹 Interface দিয়ে function-এর shape

```ts
interface MathFunc {
  (a: number, b: number): number;
}

let add: MathFunc = (x, y) => x + y;
```

---

## 🧠 কেন Interface ব্যবহার করব?

* code পরিষ্কার থাকে
* ভুল কম হয়
* বড় project সহজ হয়
* team-work ভালো হয়

---

## 🧠 এক লাইনে মনে রাখো

**Interface = TypeScript-এর নিয়মের খাতা 📘**

Object কীভাবে হবে—আগে থেকেই বলে দেয়

Interface বুঝলে TypeScript অনেক শক্তিশালী হয়ে যায় 🚀


<br><div align="center"><strong>─────── ✦ x ✦ ───────</strong></div><br><br>

# 3️⃣ Type Assertion কী?


**Type assertion** মানে হলো , 👉 **TypeScript-কে বলা: “আমি জানি এটা কোন type, তুমি আমাকে বিশ্বাস করো।”**

এটা **type বদলায় না**, 👉 শুধু TypeScript-কে **ধরে নিতে বলে**।

---

## 🍭 সহজ উদাহরণ

ধরো একটা বাক্স 📦 আছে,
আর তুমি শিক্ষককে বলছো—

👉 “ভেতরে বই আছে, তুমি চিন্তা করো না”

এই বলা-টাই হলো **type assertion**।

---

## 🔹 Example 1: `any` থেকে নির্দিষ্ট type

```ts
let value: any = "Hello World";

let length = (value as string).length;
```

👉 এখানে আমরা TypeScript-কে বলছি:

> “value আসলে string”

---

## 🔹 Example 2: HTML Element এর ক্ষেত্রে

```ts
let input = document.getElementById("name") as HTMLInputElement;

input.value = "Rahim";
```

👉 না বললে TypeScript বুঝবে না এটা input কিনা

---

## 🔹 অন্য লেখার স্টাইল (`<>`)

```ts
let value: any = "Bangladesh";

let len = (<string>value).length;
```

⚠️ React-এ এটা ব্যবহার করা হয় না
👉 তাই **`as` style বেশি safe**

---

## 🛑 Important কথা

* Type assertion **runtime-এ কিছু চেক করে না**
* ভুল type বললে **bug হতে পারে**

```ts
let num: any = "hello";
let x = num as number; // TypeScript চুপ থাকবে, কিন্তু runtime এ সমস্যা
```

---

## 🧠 কখন ব্যবহার করবে?

* `any` থেকে বের হতে
* DOM element নিয়ে কাজ করতে
* API data handle করতে

---

## ⭐ এক লাইনে মনে রাখো

**Type Assertion = “TypeScript, আমাকে বিশ্বাস করো” 🤝**
কিন্তু ভুল বললে সমস্যা তোমারই 😄

Type assertion ঠিকভাবে ব্যবহার করলে TypeScript আরও শক্তিশালী হয়ে যায় 🚀


<br><div align="center"><strong>─────── ✦ x ✦ ───────</strong></div><br><br>

# 4️⃣ TypeScript-এ কয়টি access modifier আছে?

TypeScript-এ **৩টি access modifier** আছে।

---

### 🔐 1️⃣ `public`

* ডিফল্ট modifier
* class-এর ভেতর থেকেও, বাইরে থেকেও access করা যায়

```ts
class Person {
  public name: string = "Rahim";
}

const p = new Person();
console.log(p.name); // OK
```

---

### 🛡️ 3️⃣ `protected`

* class-এর ভেতরে
* এবং **child (subclass)** থেকে access করা যায়
* কিন্তু বাইরে থেকে নয়

```ts
class Person {
  protected id: number = 1;
}

class Student extends Person {
  showId() {
    console.log(this.id); // OK
  }
}
```
---

### 🔒 2️⃣ `private`

* শুধু **class-এর ভেতরেই** ব্যবহার করা যায়
* বাইরে থেকে access করা যাবে না

```ts
class Person {
  private age: number = 20;
}

const p = new Person();
// p.age ❌ Error
```



---

### 🧠 এক নজরে পার্থক্য

| Modifier  | Class এর ভেতর | Child class | বাইরে |
| --------- | ------------- | ----------- | ----- |
| public    | ✅             | ✅           | ✅     |
| protected | ✅             | ✅           | ❌     |
| private   | ✅             | ❌           | ❌     |

---

<br><div align="center"><strong>─────── ✦ x ✦ ───────</strong></div><br><br>

# 5️⃣ Getters এবং Setters কী?

### 🔁 Getters এবং Setters কী? (TypeScript / JavaScript)

**Getter** আর **Setter** হলো class-এর **বিশেষ method**, যেগুলো দিয়ে
👉 কোনো property **পড়া (get)**
👉 আর **পরিবর্তন (set)** করা হয় **নিয়ম মেনে**।

---

## 🧠 কেন দরকার?

ধরো তুমি চাও—

* বাইরে থেকে কেউ value **সরাসরি বদলাতে না পারে**
* value বদলানোর আগে **check** হোক (যেমন negative না হয়)

এখানেই Getter–Setter কাজে আসে।

---

## 🔹 Getter কী?

👉 কোনো property-এর value **পড়ার** জন্য ব্যবহার হয়

```ts
class Person {
  private _age: number = 20;

  get age() {
    return this._age;
  }
}

const p = new Person();
console.log(p.age); // 20
```

👉 দেখো, method ডাকছি না
👉 property-এর মতো ব্যবহার করছি

---

## 🔹 Setter কী?

👉 কোনো property-এর value **set বা update** করার জন্য

```ts
class Person {
  private _age: number = 20;

  set age(value: number) {
    if (value < 0) {
      console.log("Age cannot be negative");
    } else {
      this._age = value;
    }
  }
}

const p = new Person();
p.age = 25;   // value set হচ্ছে
p.age = -5;   // condition fail
```

---

## 🔐 Getter + Setter একসাথে

```ts
class BankAccount {
  private _balance: number = 0;

  get balance() {
    return this._balance;
  }

  set balance(amount: number) {
    if (amount >= 0) {
      this._balance = amount;
    }
  }
}

const acc = new BankAccount();
acc.balance = 1000;
console.log(acc.balance); // 1000
```

---

## 🧩 গুরুত্বপূর্ণ পয়েন্ট

* `private` property সরাসরি access করা যায় না
* Getter দিয়ে **পড়া যায়**
* Setter দিয়ে **নিয়ম মেনে লেখা যায়**
* Code হয় **secure** এবং **clean**

---

## ⭐ এক লাইনে

**Getter = value পড়ার দরজা 🚪
Setter = value বদলানোর দরজা (নিয়মসহ) 🔐**

এগুলো ব্যবহার করলে class-based code অনেক বেশি নিয়ন্ত্রিত ও নিরাপদ হয়।


<br><div align="center"><strong>─────── ✦ x ✦ ───────</strong></div><br><br>


# 6️⃣ Type Alias কীভাবে define করা হয়? এবং কেন ব্যবহার করা হয়?

### 🧩 TypeScript-এ **Type Alias** কীভাবে define করা হয়? কেন ব্যবহার করা হয়?

---

## 🔹 Type Alias কী?

**Type Alias** মানে হলো 👉
**একটা type-এর জন্য নতুন নাম দেওয়া।**

এটা দিয়ে:

* primitive type
* object type
* union type
* function type
  সবকিছুর জন্য নাম বানানো যায়।

---

## 🔹 Type Alias কীভাবে define করা হয়?

### 1️⃣ Simple type alias

```ts
type ID = number;

let userId: ID = 101;
```

👉 এখানে `ID` আসলে `number`-এরই আরেক নাম।

---

### 2️⃣ Object type alias

```ts
type User = {
  name: string;
  age: number;
};

const user1: User = {
  name: "Rahim",
  age: 25
};
```

---

### 3️⃣ Union type alias

```ts
type Status = "success" | "error" | "loading";

let currentStatus: Status = "success";
```

---

### 4️⃣ Function type alias

```ts
type Add = (a: number, b: number) => number;

const addNumbers: Add = (x, y) => x + y;
```

---

## ❓ কেন Type Alias ব্যবহার করা হয়?

### ✅ 1. Code ছোট ও পরিষ্কার হয়

বারবার একই type লিখতে হয় না।

```ts
// Without alias
let u1: { name: string; age: number };
let u2: { name: string; age: number };

// With alias
type User = { name: string; age: number };
let u1: User;
let u2: User;
```

---

### ✅ 2. Complex type সহজ হয়

Union, function, nested object—সব সহজভাবে লেখা যায়।

---

### ✅ 3. Maintain করা সহজ

এক জায়গায় type বদলালেই সব জায়গায় apply হয়।

---

### ✅ 4. Error কম হয়

Type clearly defined থাকায় ভুল কম হয়।

---

## ⚠️ Type Alias বনাম Interface (সংক্ষেপে)

* **Type Alias** → union, primitive, function সব পারে
* **Interface** → mainly object structure এর জন্য

---

## ⭐ এক লাইনে

**Type Alias = Type-এর ডাকনাম 📛**
Code পরিষ্কার, ছোট আর maintainable করার জন্য ব্যবহার করা হয়।


<br><div align="center"><strong>─────── ✦ x ✦ ───────</strong></div><br><br>


# 7️⃣ public, private, protected এর পার্থক্য কী?

* **public**
  👉 সব জায়গা থেকে access করা যায় (default)

* **protected**
  👉 class এবং তার child class থেকে access করা যায়

* **private**
  👉 শুধু class-এর ভিতরে access করা যায়

---

<br><div align="center"><strong>─────── ✦ x ✦ ───────</strong></div><br><br>

# 8️⃣ any ও unknown এর পার্থক্য কী? কখন কোনটা ব্যবহার করবেন?

### 🔍 TypeScript-এ **`any`** ও **`unknown`** এর পার্থক্য

এগুলো দুটোই এমন টাইপ, যেখানে ভ্যালুর আসল টাইপ আগে থেকে জানা থাকে না। কিন্তু ব্যবহার আর নিরাপত্তায় বড় পার্থক্য আছে।

---

## 🔹 `any` কী?

`any` মানে হলো 👉 **TypeScript-এর type checking বন্ধ করে দেওয়া**।

```ts
let data: any = "Hello";

data.toUpperCase(); // OK
data();             // OK (TypeScript কিছু বলবে না)
data.xyz;           // OK
```

👉 TypeScript কোনো error দেখায় না
❌ কিন্তু runtime-এ crash হতে পারে

---

## 🔹 `unknown` কী?

`unknown` মানে হলো 👉 **টাইপ জানা নেই, কিন্তু আগে যাচাই না করলে ব্যবহার করা যাবে না**।

```ts
let data: unknown = "Hello";

// data.toUpperCase(); ❌ Error
```

ব্যবহার করতে হলে আগে check করতে হবে:

```ts
if (typeof data === "string") {
  console.log(data.toUpperCase()); // OK
}
```

👉 এটা **safe**

---

## 🧠 মূল পার্থক্য

| বিষয়          | any          | unknown      |
| ------------- | ------------ | ------------ |
| Type checking | ❌ নেই        | ✅ আছে        |
| Direct use    | ✅ করা যায়    | ❌ করা যায় না |
| Safe          | ❌ ঝুঁকিপূর্ণ | ✅ নিরাপদ     |
| Runtime error | হতে পারে     | কম হয়        |

---

## ❓ কখন কোনটা ব্যবহার করবেন?

### ✅ `any` ব্যবহার করবেন যখন:

* খুব দ্রুত prototype বানাচ্ছেন
* পুরনো JavaScript code migrate করছেন
* Temporary ভাবে type ignore করতে চান

⚠️ কিন্তু production code-এ যত কম সম্ভব

---

### ✅ `unknown` ব্যবহার করবেন যখন:

* API / external data আসছে
* user input handle করছেন
* data-এর টাইপ নিশ্চিত না

👉 আগে check করে তারপর ব্যবহার করবেন

---

## ⭐ এক লাইনে মনে রাখুন

* **`any`** → “যা খুশি করো” ❌
* **`unknown`** → “আগে যাচাই করো, তারপর ব্যবহার করো” ✅

Best practice হলো:
👉 **`any` এড়িয়ে চলুন, `unknown` ব্যবহার করুন**

---

<br><div align="center"><strong>─────── ✦ x ✦ ───────</strong></div><br><br>

# 9️⃣ TypeScript-এ null ও undefined কীভাবে handle করবেন?


## 🔹 1️⃣ `strictNullChecks` বুঝে নিন (সবচেয়ে গুরুত্বপূর্ণ)

TypeScript-এ সাধারণত **`strictNullChecks: true`** থাকে।

এর মানে:

* `null` / `undefined` **অটোমেটিকভাবে অন্য টাইপে যাবে না**
* আগে check না করলে ব্যবহার করা যাবে না

```ts
let name: string;
name = null; // ❌ Error
```

---

## 🔹 2️⃣ Union type ব্যবহার করা

যদি কোনো ভ্যালু `null` বা `undefined` হতে পারে—তাহলে সেটা স্পষ্ট করে লিখতে হবে।

```ts
let username: string | null;

username = "Rahim";
username = null; // ✅ OK
```

```ts
let age: number | undefined;
```

---

## 🔹 3️⃣ Condition check করে ব্যবহার করা

সবচেয়ে safe উপায়।

```ts
if (username !== null) {
  console.log(username.toUpperCase());
}
```

বা ছোট করে:

```ts
if (username) {
  console.log(username);
}
```

---

## 🔹 4️⃣ Optional chaining (`?.`)

যদি object বা property `undefined` হতে পারে।

```ts
type User = {
  profile?: {
    email: string;
  };
};

let user: User = {};

console.log(user.profile?.email); // undefined (error হবে না)
```

👉 না থাকলে error না দিয়ে `undefined` দেয়।

---

## 🔹 5️⃣ Nullish coalescing (`??`)

`null` বা `undefined` হলে **default value** দিতে ব্যবহার হয়।

```ts
let input: string | null = null;

let value = input ?? "Default value";

console.log(value); // "Default value"
```

⚠️ `||` আর `??` এক না:

* `||` → falsy দেখলেই বদলায়
* `??` → শুধু `null` / `undefined` দেখলে বদলায়

---

## 🔹 6️⃣ Non-null assertion (`!`) (সাবধানে)

TypeScript-কে বলা:

> “এটা null না, বিশ্বাস করো”

```ts
let el = document.getElementById("app")!;
el.innerHTML = "Hello";
```

⚠️ ভুল হলে runtime error হবে—তাই খুব সাবধানে।

---

## 🧠 কখন কী ব্যবহার করবেন?

* **Union type (`| null | undefined`)** → ভ্যালু থাকতে নাও পারে
* **Condition check** → safest
* **`?.`** → nested property
* **`??`** → default value
* **`!`** → শেষ উপায় (when you are 100% sure)

---

## ⭐ এক লাইনে

**TypeScript-এ `null` ও `undefined` handle করতে হলে—
স্পষ্ট type লিখুন, আগে check করুন, তারপর ব্যবহার করুন।**


<br><div align="center"><strong>─────── ✦ x ✦ ───────</strong></div><br><br>

# 🔟 Interface এবং Type এর পার্থক্য কী?

দুটোই TypeScript-এ **structure define** করার জন্য ব্যবহার হয়, কিন্তু ব্যবহার আর ক্ষমতায় কিছু পার্থক্য আছে।

---

## 🔹 1️⃣ Interface কী?

* মূলত **object-এর shape** define করার জন্য
* class-এ **implement** করা যায়
* একই নামের interface আবার লিখলে **merge** হয়

```ts
interface User {
  name: string;
}

interface User {
  age: number;
}

const u: User = {
  name: "Rahim",
  age: 25
};
```

👉 এটাকে বলে **declaration merging**

---

## 🔹 2️⃣ Type কী?

* যেকোনো type-এর জন্য alias বানানো যায়
* union (`|`), intersection (`&`) ব্যবহার করা যায়
* merge করা যায় না

```ts
type User = {
  name: string;
};

type Status = "success" | "error";
```

---

## 🔑 মূল পার্থক্য (সংক্ষেপে)

| বিষয়                | Interface | Type |   |
| ------------------- | --------- | ---- | - |
| Object define       | ✅         | ✅    |   |
| Primitive alias     | ❌         | ✅    |   |
| Union (`            | `)        | ❌    | ✅ |
| Intersection (`&`)  | ❌         | ✅    |   |
| Declaration merging | ✅         | ❌    |   |
| Class implement     | ✅         | ✅    |   |

---

## 🧠 কখন কোনটা ব্যবহার করবেন?

### ✅ Interface ব্যবহার করুন যখন:

* Object বা class structure define করবেন
* Large project / library design করছেন
* Extend বা merge দরকার

```ts
interface Admin extends User {
  role: string;
}
```

---

### ✅ Type ব্যবহার করুন যখন:

* Union / complex type দরকার
* Function বা primitive type alias করবেন
* Quick ও flexible definition দরকার

```ts
type ID = string | number;
```

---

## ⭐ এক লাইনে

* **Interface** → object & class structure-এর জন্য
* **Type** → সব ধরনের type-এর জন্য, বিশেষ করে union

Best practice:
👉 **Object-এর জন্য Interface,
Complex/union-এর জন্য Type**


<br><div align="center"><strong>─────── ✦ x ✦ ───────</strong></div><br><br>
